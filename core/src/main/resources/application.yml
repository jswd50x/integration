server:
  port: 9701

spring:
  application:
    name: integration-app
  datasource:
    url: jdbc:mysql://192.168.95.70:3306/integration?useUnicode=true&characterEncoding=utf8
    username: root
    password: beyondsoft
    driver-class-name: com.mysql.jdbc.Driver
    max-idle: 10
    max-wait: 10000
    min-idle: 5
    initial-size: 5

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9700/eureka/
mybatis:
  mapper-locations: classpath:mapper/*/*.xml
  type-aliases-package: com.kang.core.po
# 分布式id生成前置条件
snowflake:
  worker:
    id: 1
  datacenter:
    id: 1